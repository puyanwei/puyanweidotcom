import { Component } from '@/types'

function CVDetailed({ testId = `about-cv-detailed` }: Component) {
  return (
    <div className='grid h-screen text-gray-700 place-content-center'>
      THIS IS A PAGE FOR MY CV (MORE DETAILED)
    </div>
  )
}

export default CVDetailed
